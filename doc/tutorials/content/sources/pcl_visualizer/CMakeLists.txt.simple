cmake_minimum_required(VERSION 2.6 FATAL_ERROR)

project(pcl_visualizer_simple)

find_package(PCL 1.0 REQUIRED COMPONENTS visualization)

include_directories(${PCL_INCLUDE_DIRS})
link_directories(${PCL_LIBRARY_DIRS})
add_definitions(${PCL_DEFINITIONS})

add_executable (pcl_visualizer_simple pcl_visualizer_simple.cpp)
target_link_libraries (pcl_visualizer_simple ${PCL_VISUALIZATION_LIBRARIES})

